---
interface Props {
    name: string;
}

const { name } = Astro.props;
---

<dialog id={name} class="modal">
    <div class="modal-box max-w-[54rem]">
        <form method="dialog">
            <button
                class="btn btn-sm btn-circle btn-ghost absolute right-2 top-2"
                >✕</button
            >
        </form>

        <slot />
    </div>
</dialog>
