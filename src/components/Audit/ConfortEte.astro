---
import { Enveloppe } from "dpe-audit";
import { ConfortEte as NiveauConfortEte } from "dpe-audit-ui";
import { Alert } from "../Alert";
import { Section, SectionHeader } from "../Layout";
import { DialogButton } from "../Dialog";

interface Props {
    confort_ete?: Enveloppe.ConfortEte;
}

const { confort_ete = null } = Astro.props;
---

<Section>
    <SectionHeader title="Confort d'été">
        <DialogButton name="confort-ete" />
    </SectionHeader>

    {
        confort_ete ? (
            <NiveauConfortEte
                class="max-w-[200px] m-auto"
                value={confort_ete}
            />
        ) : (
            <Alert type="note" content="Non disponible" />
        )
    }
</Section>
