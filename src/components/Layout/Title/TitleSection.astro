---
interface Props {
    className?: string;
}

const { className = "" } = Astro.props;
---

<header class:list={["relative", className]}>
    <h2 class="grow font-medium text-xl">
        <slot />
    </h2>

    <div class="w-[80px] h-[4px] bg-primary rounded-lg mt-4"></div>
</header>
